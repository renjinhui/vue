// 托管部分
const {OpenAccount} = require('./openAccount');
const {ChangeCard} = require('./changeCard');
const {TakeNow} = require('./takeNow/takeNow.component');

module.exports = {
    OpenAccount,
    ChangeCard,
    TakeNow
}