const { PopupLogin } = require('./login');
const { InvestSucc } = require('./invest-succ');
const { PopupSuccess } = require('./success');
const { PopupResult } = require('./result');
const { PopupIsSetPwd } = require('./isSetPwd');
const { PopupTip } = require('../popup-tip.component');
module.exports = {
    PopupLogin,
    InvestSucc,
    PopupSuccess,
    PopupResult,
    PopupIsSetPwd,
    PopupTip
}