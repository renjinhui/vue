const { HY } = require('./hy.component');
const { HYOU } = require('./hyou.component');
const { HB } = require('./hb.component');
const { Tranfer } = require('./transfer.component');
const { InvestList } = require('./investList.component');

module.exports = {
    HY,
    HYOU,
    HB,
    Tranfer,
    InvestList
}