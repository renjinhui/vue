const { PopupResult } = require('./popup-result');
const { TableList } = require('./table/table-list');
const { TabList } = require('./tab/index');
const { Demo } = require('./demo');
const React = require('react');

module.exports = {
	Demo,
    PopupResult,
    TableList,
	TabList
}